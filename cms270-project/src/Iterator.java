/**
 * 
 * @author Zoe, Ebba, Carmen, Aruna
 * 
 * @version
 * This is a standard, generic interface of an Iterator,
 * which contains a hasNext() method and next() method
 * and can be used to iterate through any collection
 */
public interface Iterator {
	boolean hasNext();
	Object next();
}
